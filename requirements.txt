dvc==3.42.0
pathspec==0.12.1
pandas==2.1.4
numpy>=1.26,<2
scikit-learn==1.4.0
xgboost==2.0.3
imbalanced-learn==0.12.0
scipy==1.11.4
matplotlib==3.8.2
seaborn==0.13.1
joblib==1.3.2
pyyaml==6.0.1
jupyter==1.0.0
streamlit==1.28.1
plotly==5.18.0
altair==5.1.2
# protobuf: was pinned to ==5.24.1, which PyPI later yanked. The 5.24.x and
# 5.25.x lines are unreachable on a fresh install; available 5.x runs jump
# from 5.23.4 to 5.26.0. Relax to a range so pip's resolver picks whatever
# streamlit / mlflow / plotly mutually agree on. Day-7 CI exposed this on
# the first clean install.
protobuf>=4.25,<6

# Day 1 -- MLflow tracking + registry (sqlite-backed locally)
mlflow>=2.18,<3

# Day 2 -- Dask distributed feature engineering
dask[dataframe]>=2024.12

# Day 4 -- FastAPI serving + Pydantic schemas + telemetry store
fastapi>=0.115,<0.120
uvicorn[standard]>=0.34,<0.40
pydantic>=2.5,<3
sqlalchemy>=2,<3
# psycopg2-binary is required for the Postgres backing store (docker-compose
# service). On laptops without Postgres, the telemetry layer transparently
# falls back to sqlite — the dependency is optional in that path.
psycopg2-binary>=2.9,<3
# httpx is what FastAPI TestClient uses under the hood.
httpx>=0.25,<0.28
