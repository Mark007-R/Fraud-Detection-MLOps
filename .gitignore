# Python environment and cache
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Notebook cache
.ipynb_checkpoints/

# Large generated artifacts — anchor to repo root so src/data/ stays tracked
/data/
/models/
reports/*.svg

# Secrets and environment — keep .env.example tracked (template for contributors)
.env
.env.*
!.env.example
.streamlit/secrets.toml

# Logs
*.log

# Editor and OS files
.vscode/
.idea/
.DS_Store
Thumbs.db

# MLflow tracking (local store)
mlflow.db
mlruns/
mlartifacts/

# Sprint results (kept gitignored except json/md; large artifacts excluded)
results/samples/

# Day 4 telemetry sqlite fallback
/data/telemetry.sqlite
/data/telemetry.sqlite-*
/data/telemetry_*.sqlite

# Postgres docker volume (locally bound or for clean rebuild)
.pgdata/
